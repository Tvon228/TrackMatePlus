import classes from "./SettingsMenu.module.sass"

export default function SettingsMenu(){
    <div class={classes.container}>
        <div class={classes.content}>
            <div class={classes.header}>
                Settings
            </div>
        </div>
    </div>
}